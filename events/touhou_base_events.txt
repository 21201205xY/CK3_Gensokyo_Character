namespace = touhou_base_events

touhou_base_events.001 = { # pick your starter pokemon
	type = character_event
	title = touhou_base_events.001.t
	desc = touhou_base_events.001.desc
	content_source = dlc_touhou
	theme = holy_construction
	left_portrait = {
		character = root
		animation = personality_honorable
	}
	trigger = {
	}
	immediate = {
		save_scope_as = employer
	}
	option = {
		name = touhou_base_events.001.a
		hidden_effect = {
            create_touhou_character = { NAME = reimu }
		}	
	}
	option = {
		name = hm_bookmark.0015.b 
		hidden_effect = {
            create_character = {
                template = gsk_sagume_template
				employer = scope:employer
				save_scope_as = invited_character
            }
		}	
	}
	# option = {
	# 	name = hm_bookmark.0015.c #Z16
	# 	hidden_effect = {
    #         create_character = {
    #             template = wsg_Z16_template
	# 			employer = scope:employer
	# 			save_scope_as = invited_character
    #         }
	# 	}	
	# }
	# option = {
	# 	name = hm_bookmark.0015.d #基林
	# 	hidden_effect = {
    #         create_character = {
    #             template = wsg_Gearing_template
	# 			employer = scope:employer
	# 			save_scope_as = invited_character
    #         }
	# 	}	
	# }
	after = {
		hidden_effect = {
			if = {  # to supress error logs
				limit = {
					exists = scope:invited_character
				}
				scope:invited_character = {
					set_variable = { name = portrait_index value = 0 }
					marry = employer
				}
				set_mishu = { CHARACTER = scope:invited_character }
			}
		}
	}
}